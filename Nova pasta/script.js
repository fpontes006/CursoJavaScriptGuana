/*
let nomes = ['Whinds', 'Freeway', 'Teste', 'Maria'];

for(let n in nomes){
    console.log(`Posição: ${n} Tem o' Nome: ${nomes[n]}`)
}

function parimp(n) {
    if (n % 2 == 0) {
        return 'par'
    } else {
        return 'impar'
    }
}
let res = parimp(10)
console.log(res)
*/

